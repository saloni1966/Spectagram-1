import React from 'react';
import { View, Text } from 'react-native'

export default class CreatePost extends React.Component{
    render() {
        return (
            <View style={{flex : 1,justifyContent: 'center'}} >
                <Text style={{textAlign: 'center'}} >CreatePost !!</Text>
            </View>
        )
    }
}